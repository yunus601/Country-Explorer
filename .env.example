# Optional: REST Countries API v5 Bearer Token
# Sign up at https://restcountries.com to obtain a personal access token.
# If omitted or unconfigured, Terrascope gracefully falls back to the built-in verified global dataset.
VITE_REST_COUNTRIES_API_KEY=
